# ShopTalk — CPU-Only (Default)
# The base docker-compose.yml already runs CPU-only.
# This file exists for backwards compatibility.
#
# Usage (same result as plain `docker compose up`):
#   docker compose -f docker-compose.yml -f docker-compose.cpu.yml up -d

services: {}
